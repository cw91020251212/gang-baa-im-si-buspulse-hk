# 港巴即時修改報告

**Repository：** `cw91020251212/gang-baa-im-si-buspulse-hk`  
**報告日期：** 2026-09-16
**最近一次修改：** `a619aa1` `修正時間軸選中站點雙重框線`

## 1. 修改結論

今次工作集中處理詳細路線頁的資訊層次、定位提示、主題配色，以及底部補充資訊欄。最新版本已將時間軸的選中效果改成單一「站點內容框」。這個框只包住該站的完整資料，時間軸圓點和時間軸線留在框外，因此不會再出現外層大框加內層細框的雙重效果。

同一批相關修改亦加入了 GPS／網絡定位狀態、隧道期間的 ETA 數據推算、詳細頁控制掣整理，以及可展開的底部補充資訊欄。所有修改均集中在 `index.html`，沒有更改 API 端點或路線資料格式。

## 2. 詳細修改內容

### 2.1 時間軸選中站點的框線

原本的選中樣式會為整個 `.stop-row` 加上背景和邊框。時間軸圓點、站名、英文站名和右側狀態因此被視為同一個外層區塊。後來新增的站名內框又在 `.stop-copy` 上加了一層邊框，結果形成兩個框，亦令右側的「未有班次」等資料留在框外。

最新版本將每個站點的內容重新包入 `.stop-content`。其結構如下：

```text
時間軸圓點  [ 站名、英文站名、ETA／未有班次／落車狀態 ]
```

`.stop-content` 現在負責唯一的選中框。它會向右延伸至最右側的站點狀態，並把屬於同一車站的文字和狀態放在同一個視覺區域。`.stop-row` 本身不再顯示背景、邊框或陰影，時間軸圓點因此保持獨立。

### 2.2 明暗模式的時間軸選中樣式

黑暗模式使用低透明度綠色背景和較柔和的綠色邊框，避免亮色大面積發光。光明模式使用較淡的綠色背景和較深的綠色邊框，避免在淺灰背景上失去對比。兩種模式都套用相同的 `.stop-content` 結構，不會只修正其中一種模式。

### 2.3 詳細頁控制掣

「立即更新全部車站 ETA」已由原本的狀態區移到 GPS 控制區。GPS 操作和 ETA 更新現在位於同一個控制組內，兩個掣使用一致寬度、高度和置中方式。控制組在窄屏幕上仍會保持整齊排列，並避免文字和掣互相擠壓。

### 2.4 GPS、網絡定位及隧道後備

詳細頁現在同時監察高精度定位和低精度網絡／基站定位。系統會記錄目前定位來源和定位精度。當手機 GPS 受巴士車頂、室內環境或隧道影響時，不會立即清除最後一個可信站位。

當手機定位暫時失效，而官方 ETA 資料足夠時，頁面會按照車站順序和 ETA 時間推算巴士位於哪兩個站之間。此推算會標示為「巴士 API ETA 數據推算」，不會冒充真正的車輛經緯度。當新的 GPS 或網絡定位恢復後，系統會重新使用實際定位。

### 2.5 底部補充資訊欄

頁面底部加入了細型資訊欄。平時只顯示一行，過長內容會以省略號收納。用家撳一下資訊欄後，會展開較大的內容區，最多顯示多項補充訊息，例如：

- 某路線目前沒有班次，可能已收車；
- 網絡暫時中斷，正在顯示上次資料；
- 到站提醒已開啟；
- 已設定落車站；
- 詳情頁目前使用 GPS、網絡定位或 ETA 數據推算；
- 建議開啟 GPS 或前往設定路線的快捷操作。

資訊欄位於設定路線浮動掣上方，不會遮住右下角主要操作。快捷掣只會在資訊欄展開後出現，避免平時增加畫面雜訊。

資訊欄設有獨立的黑暗模式和光明模式配色。黑暗模式使用深藍灰背景、淺色文字和亮藍邊框；光明模式使用淺灰藍背景、深藍文字和較深邊框。兩個版本都針對文字、補充內容和快捷掣設定不同對比度。

## 3. 相關提交

| Commit | 內容 |
|---|---|
| `46ba586` | 融合 GPS 與網絡定位，並保留隧道期間的最後位置 |
| `5c7279c` | 以巴士 ETA 數據補足隧道定位 |
| `e8193e9` | 整理詳細頁控制掣及時間軸選中樣式 |
| `ce09520` | 加入可展開底部補充資訊欄 |
| `2e89e94` | 為底部資訊欄加入明暗模式配色 |
| `a619aa1` | 修正時間軸選中站點雙重框線，將站點完整資料放入單一內容框 |

## 4. 驗證結果

已對最新版本執行以下檢查：

```text
inline JavaScript syntax: OK
git diff --check: OK
```

同時已確認最新 commit 已推送至遠端 `main` 分支。GitHub Pages 會按照 repository 的部署 workflow 發佈最新內容。若瀏覽器仍顯示舊版，應先重新載入頁面；如 Service Worker 保留舊資源，則需要關閉頁面後重新開啟，或清除該網站的快取資料。

## 5. 目前限制

公開的 KMB／CTB ETA API 提供站點到站預報，但公開規格未顯示每架巴士的即時經緯度欄位。因此「巴士 API ETA 數據推算」是按站序和 ETA 得出的虛擬行車位置，不等同於巴士公司內部車載 VGPS 的實際座標。若日後取得正式車輛位置 API，應將真實車輛位置放在 ETA 推算之前使用。

## 6. 受影響檔案

本次修改的使用者介面及前端邏輯位於：

- `index.html`

本報告檔案為：

- `CHANGE_REPORT_2026-09-16.md`

## References

[1]: https://data.gov.hk/en-data/dataset/hk-td-tis_21-etakmb "Real time Arrival Data of Kowloon Motor Bus and Long Win Bus Services"

[2]: https://data.etabus.gov.hk/datagovhk/kmb_eta_api_specification.pdf "Real-time Arrival Information of KMB API Specifications"

[3]: https://www.citybus.com.hk/datagovhk/bus_eta_spi_specifications.pdf "Next Bus API Specifications of Citybus and New World First Bus"

[4]: https://www.getac.com/intl/products/software/vgps-utility/ "Getac Virtual-GPS Utility"
