# 巴士數量演算法審查

日期：2026-09-15  
審查對象：`bus_count_algorithm.js`、`eta_algorithm_description.md`  
測試路線：KMB 74X，官方 Route ETA API

## 結論

這個「ETA 時間回流」方法**有參考價值，但不能當作現時實際巴士數量**。它可以估算 ETA 序列中有幾多個「可能的行車波段」，但不能證明有幾多部實體車。

我不建議直接在介面寫成「現時有 5 部巴士行駛中」。如果要採用，應標示為「ETA 時間回流估算：5 個可能行車波段」，並提供「非車輛 GPS／非官方車輛數」說明。更穩妥的做法是先在診斷或詳細頁以低調方式顯示，經多次刷新穩定後才考慮面向一般使用者。

## 逐行檢查結果

| 原理 | 判斷 | 原因 |
|---|---|---|
| 只保留 `eta_seq === 1` | 合理但會丟失資料 | 第一班 ETA 最接近目前狀態，適合作為波段追蹤；但第二、第三班可用來交叉驗證，不能完全丟棄。 |
| 按 `seq` 車站順序排列 | 必要 | 沒有站序就不能比較 ETA 是否回流。 |
| 下一站 ETA 必須晚於上一站 | 只是假設 | ETA 是預報，不是同一部車的 GPS 軌跡；交通、取整分鐘、API 各站獨立計算都可能造成回流。 |
| 每次回流就 `busCount++` | 過度肯定 | 一次回流不一定代表新車，也可能是預報噪音、塞車差異、資料更新不同步或短線折返。 |
| 60 秒緩衝 | 可作初始參數 | 但應按資料時間、站距、ETA 誤差做校準，不應視為物理定律。 |
| 最後回傳整數車數 | 不安全 | 缺少車輛 ID、座標或行程 ID 時，只能回傳估算值及信心，不是真實車數。 |

## 官方 74X 實測

測試時間：2026-09-15 11:12（資料時間欄位為 `2026-09-15T11:12:20+08:00`）。

官方 API 回傳 74X 出發方向 `O` 的 `eta_seq === 1` 有 **16 個車站資料點**。將它們按站序排列後，供應的演算法得到：

- 有效資料點：16
- 發現時間回流：4 次
- 演算法估算：**5**
- 回流位置：第 4、7、11、13 站

這個結果與「沿線可能有多個行車波段」相符，但不能直接等同「有 5 部車」。例如某一個官方快照中，ETA 序列可以出現：

```text
站 3：11:16:26
站 4：11:12:22
站 5：11:14:20
```

這可能是前方車輛，也可能是各站預報更新不同步或 ETA 誤差。單靠時間回流，無法辨認兩種情況。

## 目前資料的關鍵限制

目前 KMB Route ETA 資料包含路線、方向、車站序號、ETA 序號、ETA 時間、備註及資料時間等欄位，但沒有可供本方法直接使用的車輛 ID、車牌、車輛座標或行程 ID。因此，演算法無法把同一部車在不同站可靠地關聯起來，也無法去除同一部車跨多站重複出現的情況。

## 建議的安全整合方式

### 第一階段：顯示估算，不宣稱實際車數

新增純函數，例如：

```js
{
  estimate: 5,
  drops: 4,
  confidence: 'low',
  label: 'ETA 時間回流估算：5 個可能行車波段',
  disclaimer: '並非官方車輛數，沒有車輛 GPS／車輛編號作確認'
}
```

不應顯示「現時有 5 部巴士」。

### 第二階段：多次刷新才提升信心

在每次 API 刷新時計算結果，保留最近 3 至 5 次快照。只有當估算值在多次刷新中接近，例如 `4, 5, 5, 5`，才顯示「估算較穩定」。若結果跳動，例如 `2, 5, 3, 6`，只顯示「ETA 預報分散」，不顯示整數車數。

### 第三階段：使用全部 ETA 序列作交叉檢查

不要只看 `eta_seq === 1`。可以用第一班作主要波段追蹤，再用第二、第三班檢查是否有不合理回流、原定班次或資料斷層。`rmk_tc` 含「原定班次／預定」的資料應降低信心或排除於 live vehicle inference。

### 第四階段：改用區間及信心，而不是單一數字

建議顯示：

> 可能有 3–5 個 ETA 行車波段（低信心；不是官方車輛數）

這比顯示「5 部車」更符合資料能力，也不會誤導使用者。

## 與目前 BusPulse 顯示邏輯的關係

目前已採用較保守的表達：相鄰而且新鮮的 ETA 足以支持途中推算時，只顯示一個站間巴士標記；多個站有近 ETA 時不逐站畫出多架巴士。這個方向是正確的。

提供的演算法可以作為日後「可能行車波段」的後端估算，但不應改變目前的站間單一巴士圖標規則，亦不應將每個時間回流直接轉成一個巴士圖標。

## 最終建議

**可以採用作研究性估算，不可以直接採用作實際巴士數量。** 下一步若要加入，應先在詳細頁的服務資料區加入低信心估算欄位、刷新穩定度及明確免責說明，並新增多快照測試；不應直接在主畫面顯示一個看似精確的巴士數字。

## 參考資料

- [KMB 官方 ETA API](https://data.etabus.gov.hk/v1/transport/kmb/route-eta/74X/1)
- [KMB ETA API 資料集及規格](https://data.gov.hk/en-data/dataset/hk-td-tis_21-etakmb)
- 使用者提供的 `bus_count_algorithm.js`
- 使用者提供的 `eta_algorithm_description.md`
