# BusPulse HK 修改紀錄

## 2026-09-07 — 加入 ETA 倒數提示 fallback

鬧鐘現在採用混合方式：每次收到 API ETA 時，除咗即時檢查提醒範圍，亦會按每班車嘅 ETA 建立本機倒數計時器。若下一次 API 輪詢錯過咗剛好進入提醒範圍嘅時間，倒數到期時會再經同一套去重邏輯嘗試發出提示。倒數只會根據最近收到嘅 ETA，仍然受手機背景暫停限制；Service Worker shell 更新至 `buspulse-hk-shell-v17`。

## 2026-09-07 — 避免輪詢延遲時漏掉第一下提示

修正部分手機或 API 輪詢較慢時，巴士已經到達提醒時間而 ETA 顯示為 `-1`／`-2` 分鐘，舊邏輯因只接受 `0` 分鐘以上而完全跳過提示的問題。現在會把仍在最近兩分鐘內的到站資料視為有效提醒範圍；同時以 `try/finally` 保證偶發的顯示或通知錯誤不會令之後的 25 秒輪詢永久卡住。Service Worker shell 更新至 `buspulse-hk-shell-v16`。

## 2026-09-07 — 原定班次繼續觸發到站鬧鐘

修正到站鬧鐘只響一次的問題。KMB ETA API 經常只為最近一班提供實時資料，後續班次則標記為「原定班次／Scheduled Bus」；舊邏輯將所有原定班次排除，導致第一班提示後下一班永遠不會再響。現在只要班次 ETA 進入使用者設定的提前提醒時間範圍，實時及原定班次都會按每班 ETA 獨立觸發提示。Service Worker shell 更新至 `buspulse-hk-shell-v15`。

## 2026-09-07 — 修正跳過一班車後下一班不再提示

到站提醒改為按每一班 ETA 時間獨立追蹤，而不是每條路線只保存一個已提醒值。當使用者錯過第一班車後，下一班進入提前提醒範圍時會重新觸發網站提示、震動及系統通知。每一班車亦使用獨立的通知 `tag`，避免手機把下一班通知當成上一張通知更新而不重新響鈴。對於實時 ETA 被 API 輕微修正的同一班車，加入 60 秒容差避免重複響鬧；已提醒紀錄只保留最近六小時及最多十二筆。

同時將 Service Worker shell cache 更新至 `buspulse-hk-shell-v14`。返回前景時原有即時刷新機制會再次檢查目前 ETA；背景測試仍須受瀏覽器是否繼續執行頁面輪詢影響。

## 2026-09-07 — 修正背景到站提示

### 問題背景

使用者反映：路線卡開啟到站鬧鐘後，當手機切到背景或使用其他 App 時，未能穩定收到「巴士就嚟到站」提示。

### 已完成修改

1. **開啟鬧鐘時主動申請通知權限**
   
   以前撳路線卡上的 `🔔` 只會開啟本地鬧鐘，但不一定會申請瀏覽器系統通知權限。現在使用者開啟鬧鐘時，程式會以這次按鈕操作作為使用者手勢，直接申請系統通知權限。

2. **改用 Service Worker 發出背景通知**
   
   到站時間進入預設提醒範圍後，程式會把通知資料交給已註冊的 Service Worker，再由 Service Worker 呼叫系統通知。這比只由目前頁面直接建立通知，更適合處理切換到背景的情況。

3. **重新開啟鬧鐘時清除舊提醒狀態**
   
   開關鬧鐘時會清除該路線的舊 `alarmSeen` 記錄，避免重新開啟後因為殘留狀態而跳過下一次提醒。

4. **更新 Service Worker 快取版本**
   
   快取名稱由 `buspulse-hk-shell-v3` 更新為 `buspulse-hk-shell-v4`，令瀏覽器在部署後重新下載修正版 `index.html`。

### 測試及驗證

- `index.html` 內嵌 JavaScript 語法檢查：通過。
- `sw.js` Node 語法檢查：通過。
- `git diff --check`：通過，沒有空白格式錯誤。
- GitHub Actions Pages 部署：成功。
- 部署提交：`f96fe6f`（`Fix background arrival notifications`）。
- 線上 Service Worker 快取版本：已確認為 `buspulse-hk-shell-v4`。
- 原始碼遠端版本：已確認包含 `requestNotifications()`、Service Worker `postMessage` 通知通道及 `alarmSeen.delete(id)`。

### 使用者測試步驟

1. 開啟 <https://cw91020251212.github.io/gang-baa-im-si-buspulse-hk/>。
2. 如使用主畫面 App，先完全關閉並重新開啟，讓新的 Service Worker 生效。
3. 在任一路線卡撳右上角 `🔔`。
4. 允許系統通知權限；如果之前按過「不允許」，需要到手機瀏覽器的網站通知設定重新允許。
5. 將網站切到背景，再觀察到站時間進入「提前幾多分鐘」的設定範圍時，是否出現系統通知。

### 已知限制

這個專案是純前端 PWA，ETA 是每 25 秒向官方 API 輪詢一次。手機完全鎖屏、強制停止瀏覽器、作業系統省電模式暫停網頁，仍可能令 JavaScript 輪詢、音效或震動停止。Service Worker 可以改善系統通知的背景顯示，但不能保證在所有手機省電及鎖屏狀態下都運作。

另外，官方 ETA API 提供預計到站時間，不是每架巴士的公開推送事件，因此提醒時間仍以 API 當時回傳資料為準，並非保證精確到秒。

### 回退／追查方法

如果今次測試仍然失敗，請記錄以下資料再交回檢查：

- 使用手機型號及瀏覽器名稱／版本。
- 是否已允許網站通知。
- 是切到背景仍有頁面開啟，還是完全關閉／鎖屏後測試。
- 路線卡上的 `🔔` 是否顯示為開啟狀態。
- 測試時設定的提前分鐘數。
- 當時頁面最後一次更新時間及 ETA 顯示。
- 是否有收到卡片閃爍、聲音、震動其中任何一種提示。

## 部署位置

- Repository：<https://github.com/cw91020251212/gang-baa-im-si-buspulse-hk>
- 網站：<https://cw91020251212.github.io/gang-baa-im-si-buspulse-hk/>

## 2026-09-07 — 加入夜間測試通知

### 修改內容

- 在「顯示與提醒設定」加入「測試背景通知」按鈕。
- 測試按鈕會先申請系統通知權限，再經由 Service Worker 發出一個模擬到站通知。
- 測試通知不會呼叫巴士 API、不會更改路線、不會開啟真實鬧鐘；只用來確認手機的背景通知通道。
- Service Worker 快取版本更新至 `buspulse-hk-shell-v5`。

### 測試方法

開啟網站 → 按齒輪 →「顯示與提醒設定」→「測試背景通知」。收到「港巴即時測試提示」後，可以切到其他 App 或鎖屏，再進行真實鬧鐘測試。

## 2026-09-07 — 改為延遲背景測試

即時撳掣只可以測試前景通知，不能證明程式放到背景後仍然會提示。因此測試功能改為可選 **15 秒後** 或 **1 分鐘後** 發出通知。使用者撳「安排背景測試」後，應立即切去其他 App 或鎖屏，等時間到才觀察是否收到「港巴即時背景測試」。Service Worker 快取版本更新至 `buspulse-hk-shell-v6`。

此測試仍然是瀏覽器內的延遲計時器；如果手機完全暫停或強制停止瀏覽器，計時器可能延遲或停止。要做到程式完全關閉後仍可靠地定時推送，需要伺服器推送通知或作業系統原生鬧鐘服務，單靠 GitHub Pages 純前端不能保證。

## 2026-09-07 — 強化背景測試倒數顯示

- 將倒數秒數移到「安排背景測試」按鈕旁邊，不再只放於下方說明文字。
- 使用高對比白底紅字徽章顯示 `15s`、`14s`、`13s` 等倒數，並在待機／完成時顯示不同狀態。
- Service Worker 快取版本更新至 `buspulse-hk-shell-v7`。

## 2026-09-07 — 記住系統通知權限狀態

通知權限由瀏覽器按網站來源保存，不需要每次重新按允許。設定頁現在會讀取瀏覽器記錄：已批准時顯示「系統通知已允許 ✓」並停用按鈕；尚未決定時顯示允許按鈕；曾經封鎖時提示到瀏覽器設定開啟。開啟路線鬧鐘及安排延遲測試時，如果權限已批准，會直接使用而不會再次要求。Service Worker 快取版本更新至 `buspulse-hk-shell-v8`。

## 2026-09-07 — 加強到站提示辨識度

到站鬧鐘現在會連續播放三輪較長提示旋律，並延長震動節奏；前景頁面會令頂部列及狀態燈閃動約 9 秒，路線卡亦會維持原有閃動。其他 App 的畫面不能由網站直接令其閃動，背景時主要依靠 Android／瀏覽器系統通知、聲音及震動。使用者應按 Home 將網站留在背景，不應撳返回或完全關閉頁面；純前端網頁在完全關閉、被系統強制停止或省電模式暫停時不能保證繼續輪詢。Service Worker 快取版本更新至 `buspulse-hk-shell-v9`。

## 2026-09-07 — 加入可調校提示聲

設定頁新增提示聲控制。使用者可以選擇「兩下叮噹」、「三下提示」或「鬧鐘長響」，再選擇較短、標準或較長，並可按「試聽目前提示聲」。選擇會保存到本機瀏覽器，下次開啟仍會保留。背景狀態下，實際通知聲效仍由手機的通知音量及系統通知設定控制；網站可以控制前景 Web Audio、通知內容及震動。Service Worker 快取版本更新至 `buspulse-hk-shell-v10`。

## 2026-09-07 — 加入通知及 App 圖標徽章測試

延遲背景測試現在會明確使用「【測試】港巴即時背景提示」標題，方便分辨測試與真實到站通知。發出通知時會嘗試在支援 Badging API 的手機主畫面 App 圖標顯示數字 `1`；按下通知後會清除徽章。真實到站提醒同樣會嘗試顯示數字 `1`。如果手機或瀏覽器不支援數字徽章，仍可收到頂部系統通知，但圖標數字由作業系統決定是否顯示。Service Worker 快取版本更新至 `buspulse-hk-shell-v11`。

## 2026-09-07 — 修正背景測試聲效及移除小窗選項

延遲背景測試在計時到期時，現在會先播放使用者選擇的兩下、三下或鬧鐘聲效，再發出標示為「【測試】」的系統通知；安排測試時會先解鎖音效，方便測試網站留在背景時的實際效果。不過手機完全暫停背景頁面時，瀏覽器仍可能阻止網頁音訊，系統通知聲則繼續由手機通知設定控制。

由於 Document Picture-in-Picture 在手機上支援度低，設定頁已移除「開啟最上層小窗」按鈕及說明，避免誤導使用者。Service Worker 快取版本更新至 `buspulse-hk-shell-v12`。

## 2026-09-07 — 可選只用系統通知聲

修正聲音選項方向。設定頁現在可以揀「網站聲效＋系統聲」或「只用系統通知聲」。揀「只用系統通知聲」後，網站不會播放兩下、三下或鬧鐘自訂聲，但仍會保留手機頂部系統通知、系統通知聲及通知徽章。原有系統通知靜音欄位不再顯示，避免同時出現相反意思的選項。Service Worker 快取版本更新至 `buspulse-hk-shell-v13`。

## 2026-09-07 — 整理測試工具

將提示聲款式、提示聲長度、聲音來源、試聽提示聲、延遲背景測試、測試倒數及測試狀態集中放入設定頁的可收合「測試工具」區塊。日常設定頁只保留提前提醒時間、系統通知權限及外觀選項，避免測試控制項散落並干擾一般使用。

本次提交：`9915863 Group diagnostic controls under test tools`。原有測試功能及 HTML element IDs 保留，事件處理不變；只調整設定頁分組及相關說明文字。JavaScript syntax、`git diff --check` 及 GitHub Pages 部署均已通過。

## 2026-09-07 — 修正長距離路線地圖直線穿越

檢查 73X 由大埔往荃灣的地圖後，確認最新背景通知系列提交沿用了一次性全路線 OSRM 查詢；當路由器失敗或回傳不合理結果時，原有 fallback 會直接以站點座標直線連接，因而出現穿山、穿海的錯誤藍線。今次只從 `d76bdc6` 合併長距離分段路由修正，將相距超過 5 公里的相鄰站頭逐段交給 OSRM，並保留最新背景通知及測試功能不變。

本次提交：`fd83a0c Route long map gaps segment by segment`。這是地圖專用修改，沒有改動 `notifyArrival()`、`checkAlarms()`、Service Worker 通知或背景輪詢；若之後背景提示出錯，應先比較通知相關提交，而不是回退這個地圖修正。
