# BusPulse 路線詳情頁 UX 研究與規格

**階段：** 2 — 全畫面路線詳情、逐站 ETA 與途中巴士狀態  
**日期：** 2026-09-15  
**範圍：** 只研究與定義 UI、狀態、互動及驗收；本階段不修改程式。

## 1. 研究結論與參考

Moovey 的 KMB 74X 路線頁提供一個適合手機優先的參考骨架：頂部先顯示路線號碼、來回方向、站數、首尾班及全程車資；其後顯示「目前顯示車站」，以三格即時區呈現即將到達／下一班／第三班；再以完整「沿途車站列表」作為主要內容，點擊站點可切換觀察點。Moovey 同時提供無即時資料、服務狀態、當日尾班已過及暫停提供等文字，而不是以假 ETA 填充空位。[Moovey 路線頁][1]

Moovit 的產品描述亦將路線、地圖、live arrival、剩餘站數及服務警報放在同一個行程脈絡中；Transit 則強調點擊路線後查看站點列表、live departure 及 vehicle tracking。[Moovit][2] [Transit][3] 這些產品可作資訊架構參考，但 BusPulse 必須比照本專案的資料限制：官方 ETA 通常沒有巴士車輛座標，故不可把 ETA 推算的巴士位置說成 GPS 實時位置。

專案現有資料與限制：已有路線卡、站點 ETA、地圖、GPS 落車提醒、鬧鐘及明暗主題；`routeStopsCache` 可支援站點列表。官方 ETA 可支援逐站查詢，但沒有普遍公開的每架巴士即時 GPS；現有地圖藍線是道路模擬，藍色圓點是使用者位置，不是巴士位置。[AI handoff][4]

## 2. 產品目標與非目標

### 2.1 目標

1. 使用者由任何路線小卡一按即可進入獨立、可返回的全畫面路線詳情頁。
2. 使用者可從完整站點時間線點擊任何站，頂部即時區立即切換至該站的三個 ETA。
3. 以清晰、可掃讀的時間線表達已過站、目前觀察站及後續站點。
4. 當資料足夠時，在兩個站點之間放置「推算中的巴士」圖標，協助理解行程進度，但明確標示為 ETA 推算。
5. 明暗主題只改變色彩 token，不改變資訊層級、文案或互動。
6. 點擊路線卡、站點或切換站點只查詢及顯示資料，**不可意外啟動 GPS、鬧鐘、聲音、震動或通知權限**。

### 2.2 非目標

本頁不是地圖導航、GPS 車輛追蹤、落車提醒設定頁或完整班次表。GPS、鬧鐘及聲音應保留為明確、獨立的既有操作；本頁不因載入、選站或滾動而自動開啟它們。

## 3. 全畫面資訊架構

由上至下建議如下，手機寬度以 360–430 px 為主要設計基準：

### A. 頂部導覽列（sticky）

左側為返回按鈕（44×44 px 觸控區，文案/無障礙名稱「返回路線列表」）；中央顯示路線號碼及營辦商短標籤，例如「KMB 74X」；右側只放收藏狀態或更多資訊，不放 GPS 或鬧鐘快捷鍵。返回後應保留原本列表滾動位置及已選站。

### B. 路線摘要卡

顯示方向「觀塘碼頭 → 大埔中心」、站數「20 站」、首班／尾班及官方全程車資。方向切換必須是明確按鈕（「對調方向」），觸發後重新載入該方向的站點及 ETA，並將選站設為新方向首站；不可因點擊摘要文字而切換。

### C. 目前觀察站與三格 ETA

標題固定為「目前顯示車站」，下面顯示所選站中文名、站號及英文副名。三格從左至右為「即將到達／下一班／第三班」，每格包含大字分鐘值及絕對時間（如有）。三格必須有固定欄寬和相同高度，避免 ETA 更新造成頁面跳動。所選站改變時只替換此區資料，並在站點列上顯示選中狀態。

每格文案需保持資料語意：可顯示「3 分鐘」「12 分鐘」及「10:42」；不可把「未有資料」顯示成 0 分鐘。更新時間顯示為「更新於 10:39」或「資料約 2 分鐘前」，並在過期時降級為「資料可能已過期」。

### D. 非阻塞服務狀態條

在 ETA 三格下方顯示服務、錯誤或資料新鮮度：例如「實時資料 · 剛更新」「連線失敗 · 顯示上次資料」「未有實時數據 · 以下為時刻/服務狀態」。錯誤不可覆蓋整頁站點時間線；若重試，按鈕文案明確為「重新載入」，且不請求 GPS 或通知權限。

### E. 完整站點時間線

標題「沿途車站列表（共 20 個站點）」；每行包括順序、中文站名、英文站名/站號及可選的站點 ETA 摘要。每行整體為按鈕，最小觸控高度 56 px；點擊行即可切換頂部觀察站，不需另設小型按鈕。選中行應有高對比邊框/底色、左側選中指示及「已選」無障礙狀態。

時間線由垂直主線、圓點及站點列組成。起點和終點可使用較重字重；分段收費、轉車站等營運資訊以次要文字顯示，不能搶過站名和 ETA。站點列表必須支援長頁滾動；進入頁面時可把選中站置於視窗附近，但不可自動開 GPS。

### F. 「推算中的巴士」位置

若同一路線方向至少有兩個相鄰站點具備可信、同一更新批次的 ETA，且 ETA 單調遞增，將巴士圖標放在相鄰兩站的線段中間或按比例位置。圖標旁顯示「推算中」；輔助文字為「按站點 ETA 推算，並非巴士 GPS」。位置應以離前站／後站的相對 ETA 插值而非地圖座標，並容許顯示在「目前站前／後」的線段。

若只有一個站有資料、ETA 不單調、資料時間過期、服務狀態不明或資料是舊快取，隱藏巴士圖標，改在服務狀態條顯示「暫無足夠資料推算巴士位置」。絕不顯示看似精準的 GPS 圓點、速度或道路軌跡。

## 4. 明暗主題視覺規格

使用語意 token 而非直接寫死顏色：`bg`（頁面底色）、`surface`（卡片）、`surfaceRaised`（ETA 卡）、`textPrimary`、`textSecondary`、`border`、`accent`、`success/approaching`、`muted/past`、`warning/stale`、`error`。灰黑主題以深灰底、稍亮卡片、白色主字及青綠/藍綠強調色；光明主題以近白底、白卡、深灰字及同一色相強調色。不可只靠顏色區分狀態，需同時有文案、圖示形狀或線型。

- 主要文字最少 16 px；ETA 數值 24–32 px、tabular numerals。
- 次要英文/站號可 12–14 px，保持足夠對比。
- 所有按鈕及站點列至少 44×44 px；不以窄小圖標作唯一操作入口。
- 深色模式避免純黑與純白的大面積對撞；光明模式避免低對比灰字。
- `prefers-reduced-motion` 時，ETA 更新不可閃爍或大幅位移；只用短暫邊框/底色變化。

## 5. 狀態模型

建議將資料狀態與視覺狀態分離：

```text
RouteDetailState {
  route: { operator, number, direction, stops[], fare, firstTrip, lastTrip },
  selectedStopId,
  etaByStop: { [stopId]: { trips[], fetchedAt, source, freshness } },
  vehicleInference: { status, fromStopId?, toStopId?, progress?, reason? },
  view: { loading, error, offline, theme },
  sideEffects: { gps: unchanged, alarm: unchanged, sound: unchanged }
}
```

### 5.1 站點視覺狀態

| 狀態 | 視覺及文案 | 點擊行為 |
| --- | --- | --- |
| 未到站／後續站 | 普通主線與圓點，站名和可用 ETA 摘要 | 設為 `selectedStopId`，更新頂部三格 |
| 已過站 | 主線及文字降為 muted，圓點可用空心/勾號；文案「已過站」 | 仍可查看該站歷史/當前可得 ETA，不可阻止選取 |
| 目前觀察站 | accent 邊框/底色、實心圓點、「已選」 | 不重複觸發副作用；可重新整理資料 |
| 接近中 | 只在 ETA 小於產品定義門檻（例如 5 分鐘）且資料新鮮時顯示「接近中」；可用 accent 圓點及「約 3 分鐘」 | 只更新展示，不自動開提示聲/震動/GPS |
| 無資料 | ETA 位置顯示「未有資料」，附「暫無即時班次」；不顯示 0 | 仍可選取；重試為明確按鈕 |
| 資料過期 | 顯示上次數值但加「資料約 N 分鐘前／可能已過期」及 warning；不可標記為接近中 | 可手動重新載入 |
| 終點站 | 終點標籤；若當日班次完結顯示「當日尾班已過」 | 可選取查看服務狀態 |

### 5.2 三格 ETA 狀態

每個格子的資料可為 `arrival`（分鐘及時間）、`no-data`、`offline`、`ended`、`stale` 或 `error`。離線時顯示「離線」/「上次資料」，不可把快取數值當作新鮮實時資料；當日尾班已過時三格顯示「已收車」或「當日尾班已過」；API 失敗時顯示「讀取失敗」並提供重新載入。

### 5.3 巴士推算狀態

`hidden`：資料不足或不可信；`inferred`：位於 `fromStopId` 與 `toStopId` 之間，顯示巴士圖標和「推算中」；`stale`：曾可推算但資料過期，移除圖標並保留說明；`invalid`：ETA 順序矛盾或跨方向，移除圖標並記錄原因供除錯。任何狀態均不得命名為 `gps` 或使用「巴士目前位置」等會誤導的文案。

## 6. 互動流程

### 流程 A：由細卡進入

1. 使用者點擊路線卡主體或明確「查看完整路線」區域。
2. 系統開啟全畫面詳情頁，保留路線方向與卡片所選站；先渲染摘要及快取站點，再非阻塞更新 ETA。
3. 頁面只讀取資料；不呼叫地理定位、不播放聲音、不排程鬧鐘、不要求通知權限。
4. 使用者按返回，回到原列表位置；瀏覽器返回鍵/手勢行為一致。

### 流程 B：點擊任何站

1. 使用者點擊任何站點整行。
2. 行被標記為選中；頂部觀察站名稱、三格 ETA、更新時間及狀態同步替換。
3. 若站點不在視窗中，頁面只需將頂部區保持可見；不得把使用者強行跳回頁頂造成迷失。可提供明確「查看目前站」按鈕，但非必要。
4. API 請求應可取消或去重；快速連點時最後一次選站勝出。
5. 點擊結果不可連帶開啟 GPS、鬧鐘、聲音、震動或通知。

### 流程 C：方向切換

1. 使用者點擊「對調方向」。
2. 明確替換方向、站點序列及選中站（預設新方向起點）；保留全畫面位置。
3. 在載入期間顯示 skeleton/載入狀態，不把舊方向站名與新方向 ETA 混在一起。

### 流程 D：資料錯誤或離線

保留站點骨架與最後有效更新時間，於頂部顯示可理解的錯誤條；提供「重新載入」。沒有資料就顯示空狀態，不臆測班次。離線狀態不可讓使用者以為數值仍為即時；恢復連線後才更新 freshness。

### 流程 E：與 GPS、鬧鐘的邊界

GPS 只能由既有、明確標示「使用我的位置／附近站」的控制觸發，且必須在權限前說明用途；本頁載入、選站及捲動均不得呼叫 `geolocation`。鬧鐘只能由既有明確「設定到站提醒」控制觸發，並先顯示站點、班次及提前分鐘確認；三格 ETA、巴士圖標及「接近中」只作資訊展示，不自動排程 alarm、聲音、震動、badge 或通知。若未來在本頁加入這兩種控制，必須獨立放在按鈕或底部工作表，不能藏在站點行點擊內。

## 7. 無障礙與內容規則

站點行使用按鈕語意並提供完整讀屏名稱，例如「第 7 站 牛頭角下邨，未來約 8 分鐘，按兩下查看」。ETA 更新以 `aria-live="polite"` 宣告摘要，不逐秒讀出。選中站有 `aria-current="true"`；已過站不可只依賴灰色表示。鍵盤使用者可按 Tab 依序經過返回、方向、重新載入、站點行；焦點環在明暗主題都清楚可見。所有時間需同時有相對分鐘及絕對時間（資料有提供時），語言採香港繁體中文/自然廣東話 UI，英文站名作輔助。

## 8. 驗收準則

### 導航與結構

- [ ] 任一路線小卡都可用一次點擊進入全畫面詳情；可返回原列表位置。
- [ ] 詳情頁固定顯示路線、方向、站數、首尾班及官方全程車資（資料有提供時）。
- [ ] 站點列表完整、順序正確、包含站名及站號/英文輔助資訊。

### 站點與 ETA

- [ ] 點擊任一站後，頂部觀察站在一個互動回合內切換。
- [ ] 三格固定顯示即將到達、下一班、第三班；缺資料時顯示明確狀態而非 `0`、假數值或空白。
- [ ] 過站、接近中、無資料、過期、離線、尾班已過及 API 錯誤均有可理解文字及非顏色辨識。
- [ ] 快速切換站點不會將舊站 ETA 顯示在新站標題下。

### 巴士圖標與真實性

- [ ] 只有兩個以上相鄰且同批次、可信 ETA 才顯示巴士圖標。
- [ ] 圖標位於兩站之間並標示「推算中」及「並非巴士 GPS」。
- [ ] 資料不足、過期或順序矛盾時不顯示圖標，改顯示原因。
- [ ] 任何文案、圖例或無障礙名稱都不把推算位置稱作實時 GPS。

### 主題、觸控與效能

- [ ] 灰黑及光明主題均有足夠文字、邊框、焦點及狀態對比，且資訊層級不變。
- [ ] 所有站點行及控制最少 44×44 px；頁面可在 360 px 寬度無橫向滾動。
- [ ] ETA 更新不造成大幅版面跳動；偏好減少動畫時不閃爍。
- [ ] 載入中先顯示穩定骨架，失敗時不白屏。

### 副作用隔離

- [ ] 進入詳情、點擊站點、切換方向、重試及捲動均不會呼叫 GPS 權限。
- [ ] 上述操作均不會建立/修改鬧鐘、播放聲音、震動、設定 badge 或要求通知權限。
- [ ] 只有明確的既有 GPS/鬧鐘控制才可觸發相應功能，且有確認文案。

## 9. 實作前資料與風險備註

逐站 ETA 可沿用現有 KMB、城巴及 GMB ETA 解析流程，但需以 route/方向/stop key 做資料隔離，避免切站競態。推算巴士只應作 UI 層的保守插值，不應新增「真實車輛位置」資料模型。資料新鮮度門檻及「接近中」分鐘門檻應由產品常數集中管理並可測試。若 API 只回傳單站 ETA，應保留完整站點時間線但隱藏推算圖標。任何快取、離線或尾班狀態都必須顯示來源與時間，避免使用者把舊數值當成保證。

## 參考資料

[1]: https://moovey.net/route/KMB-74X-I-1.html "Moovey KMB 74X 路線詳情頁（研究時讀取）"
[2]: https://apps.apple.com/hk/app/moovit-public-transport-live/id498477945?l=en-GB "Moovit App Store 產品描述"
[3]: https://apps.apple.com/us/app/transit-subway-bus-times/id498151501 "Transit App Store 產品描述"
[4]: ./AI_HANDOFF.md "BusPulse HK AI handoff：現有功能與官方資料限制"
