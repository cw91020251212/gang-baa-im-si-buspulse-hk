# 港巴即時（BusPulse HK）起點／終點路線搜尋交接文件

## 文件用途

本文件供其他人工智能或開發者接手 BusPulse HK 的「由起點搵終點」功能時參考。修改前應先閱讀本文件，並確認目前 GitHub `main` 分支及工作區版本，避免將較舊版本重新覆蓋較新版本。

## 專案及公開網址

專案名稱是「港巴即時（BusPulse HK）」。GitHub repository 為 `cw91020251212/gang-baa-im-si-buspulse-hk`，公開網站是：

<https://cw91020251212.github.io/gang-baa-im-si-buspulse-hk/>

目前與起點／終點搜尋相關的最後提交是 `e897c6c`。如要進行後續修改，先執行：

```bash
git fetch origin
git status
git log --oneline --decorate -10
```

## 目前已完成的功能

在「加新路線」畫面加入了「⌖ 由起點／終點搵路線」入口。原本的路線號碼搜尋沒有移除，使用者仍然可以輸入 74、74K、74X 等路線號碼搜尋。

目前起點／終點搜尋流程是：

```text
加新路線
→ 由起點／終點搵路線
→ 輸入起點地區／地標
→ 輸入終點地區／地標
→ 系統掃描官方站點索引
→ 顯示候選直達路線
→ 使用者揀路線
→ 接回原有揀方向／揀站頭流程
```

候選結果會顯示路線號碼、行車方向、最接近起點的站頭、最接近終點的站頭，以及兩個位置的大約距離。揀選候選結果後，會進入原有的站頭選擇、地圖、ETA、車費、鬧鐘及落車提醒流程。

## 目前資料來源

`place-index.json` 是今次新增的官方站點索引。它由運輸署公開的九巴／龍運 API 產生，包含約 1,499 個路線方向及站點資料。索引內每個站點包括：

```json
{
  "id": "官方站點 ID",
  "seq": 1,
  "name": "站頭中文名",
  "lat": 22.45,
  "lng": 114.16
}
```

每個路線方向亦包含：

```json
{
  "co": "KMB",
  "route": "72",
  "bound": "O",
  "service_type": "1",
  "orig_tc": "大埔",
  "dest_tc": "長沙灣",
  "stops": []
}
```

目前第一版主要支援九巴／龍運的地區搜尋。城巴、新巴及綠色小巴仍然可以使用原本的路線號碼搜尋，尚未加入相同的地區批量索引。

## 目前搜尋邏輯

目前程式在 `index.html` 內的主要函式包括：

- `geocodeHK(q)`：將輸入的地區／地標轉換成座標。常用香港地區有內置座標 fallback，其他輸入會嘗試使用 Nominatim。
- `distanceKm(a, b)`：以近似地理距離計算兩點距離。
- `searchPlaceRoutes(fromQ, toQ)`：掃描 `place-index.json`，每條路線目前只取一個最接近起點的站及一個最接近終點的站。
- `viewPlaceSearch()`：顯示起點／終點輸入介面。
- `doPlaceSearch(fromQ, toQ)`：執行搜尋並顯示結果。

目前候選條件是：

1. 起點最近站距離輸入位置不超過約 3.5 公里。
2. 終點最近站距離輸入位置不超過約 3.5 公里。
3. 起點站的 `seq` 必須早於終點站的 `seq`。
4. 結果按起點及終點距離總和排序。
5. 最多顯示 60 個結果。

## 下一步建議改造方向

目前版本是可用的第一版，但搜尋結果仍然將所有候選路線一次過列出。下一版應改成分步驟配對，令使用者有更多選擇而又不會被大量結果混亂。

建議流程如下：

```text
第一步：輸入起點地區／地標
→ 顯示起點附近候選站頭及經過路線
→ 使用者可先揀一個起點站，亦可暫時保留多個候選

第二步：輸入終點地區／地標
→ 顯示終點附近候選站頭及經過路線
→ 使用者可先揀一個終點站，亦可保留多個候選

第三步：系統配對
→ 檢查同一路線及同一方向是否先經起點、後經終點
→ 按結果分類顯示
```

配對結果應分成以下幾組：

### 直達路線（最推薦）

同一條路線、同一方向，站序由起點到終點正確，並且兩個站都在合理距離內。

### 起點及終點附近路線

兩邊都有站，但距離較遠，或者需要由附近位置步行一段路。這些結果仍然可以顯示，但要清楚標註距離。

### 只符合起點

路線經過起點附近，但沒有到達終點附近。這類結果應放在「更多可能路線」，不要與直達結果混在一起。

### 只符合終點

路線到達終點附近，但沒有經過起點附近。同樣應放在較低優先級，不要令使用者誤會是直達路線。

## 重要技術改造建議

目前每條路線只保留一個最近起點站及一個最近終點站，可能錯過更適合的站頭。下一版應保留每邊最多三個候選站：

```text
startCandidates = 起點附近距離最短的最多 3 個站
endCandidates = 終點附近距離最短的最多 3 個站
```

然後檢查所有組合：

```text
for each start in startCandidates:
  for each end in endCandidates:
    if start.seq < end.seq:
      建立候選配對
```

結果應該保存：

```js
{
  route,
  co,
  bound,
  dir,
  origin,
  dest,
  startStop,
  endStop,
  startDistance,
  endDistance,
  stopCount,
  category,
  score
}
```

不要只保存一個 `placeStart` 及一個 `placeEnd`，否則使用者無法在同一路線內選擇其他合理的上下車站。

## 介面建議

第一頁應顯示：

```text
起點：大埔

附近上車選擇：
○ 太和總站（0.4 km）
○ 太和站（0.6 km）
○ 大埔中心（1.1 km）

[繼續揀終點]
```

第二頁應顯示：

```text
終點：沙田

附近落車選擇：
○ 沙田市中心總站（0.2 km）
○ 帝都酒店（0.8 km）
○ 乙明邨（1.0 km）

[顯示可能路線]
```

結果頁應按組別顯示，而不是將所有結果混為一個長列表：

```text
直達路線（2）
72    太和總站 → 帝都酒店
74A   太和總站 → 乙明邨

起點及終點附近（4）
...

更多可能路線（8）
...
```

每張結果卡應包括：

- 路線號碼
- 行車方向
- 上車站及距離
- 落車站及距離
- 中間站數量
- 是否直達
- 「選擇」按鈕

## 不應該做的事情

不要將所有起點及終點候選路線一次過無分類顯示。不要把只經過起點或只經過終點的路線標成直達。不要移除原本的路線號碼搜尋。不要將城巴／小巴資料硬套用九巴欄位。不要在沒有重新檢查 Git 歷史的情況下使用舊版本覆蓋現有 `main` 分支。

## 第二階段功能

以下功能可以在分步驟直達搜尋穩定後再考慮：

- 城巴／新巴起點終點索引
- 綠色小巴起點終點索引
- 轉車路線
- 轉車時間及轉車站推薦
- 兩程車費合計
- 起點／終點地圖選擇
- 使用者指定「最多步行距離」
- 使用者指定「最多站數」
- 使用者指定「只接受直達」

## 修改後驗證清單

每次修改後至少執行：

```bash
cd /home/ubuntu/workspace
node --check <抽出的 JavaScript 檔案>
git diff --check
test -s index.html
test -s place-index.json
git status
```

亦應實際測試以下例子：

```text
大埔 → 沙田
旺角 → 尖沙咀
荃灣 → 觀塘
```

要確認：

- 有結果時可以逐條選擇
- 沒有結果時顯示清楚訊息
- 起點站序確實早於終點站序
- 選擇路線後可以接回原有揀站頭頁面
- 原本輸入路線號碼功能仍然正常
- `place-index.json` 載入失敗時有清楚提示

## 目前部署紀錄

本次起點／終點搜尋初版的主要提交是 `eb5f46d`；交接文件提交為 `e897c6c`。任何後續改動應先從遠端最新 `main` 開始，並在提交訊息中說明是否修改搜尋邏輯、資料索引或介面流程。
