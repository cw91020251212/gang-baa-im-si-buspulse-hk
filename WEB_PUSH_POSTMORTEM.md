# Web Push 鬧鐘工程失誤檢討與移除紀錄

日期：2026-09-14  
專案：BusPulse HK／港巴即時  
狀態：Web Push 已由正式程式架構移除；本地倒數及本地鬧鐘保留。

## 1. 最終工程決定

本專案的 Web Push 沒有實際存在價值，已予以移除。原因是現有本地鬧鐘已能完成原本的到站提醒目的，Web Push 沒有增加另一項必要能力，卻增加了訂閱、授權、VAPID、Worker、伺服器同步、資料清理及瀏覽器相容性等複雜度。

本專案現時只保留以下提醒路徑：

| 保留項目 | 作用 | 是否依賴 Web Push |
|---|---|---:|
| ETA 資料取得 | 從官方交通資料 API 取得到站資料 | 否 |
| 本地倒數 | 按 ETA 計算剩餘時間 | 否 |
| 本地鬧鐘計時器 | 按已取得的 ETA 安排提醒 | 否 |
| Service Worker 本地通知 | 由頁面透過本地訊息顯示通知 | 否 |
| Web Push Worker | 在頁面外查詢及推送通知 | 已移除 |

## 2. 原本的錯誤判斷

最根本的錯誤是沒有先問清楚 Web Push 在產品中要完成甚麼不可替代的工作。Web Push 當初是為鬧鐘而加入；如果本地鬧鐘已經能完成提醒，Web Push 就不再是必要功能。正確的工程問題應是「本地鬧鐘是否已完成需求」，而不是「Web Push 能否再找一個用途」。

第二個錯誤是把「技術端點可以回應」誤當成「產品功能有價值」。Worker 的 `/vapid-public-key` 可以回應、瀏覽器可以建立 Push subscription、單元測試可以通過，最多只能證明部分程式路徑存在，不能證明鬧鐘在真實手機、鎖屏、背景、省電模式及不同瀏覽器下可靠完成。技術上可連線，不等於使用者實際得到可靠鬧鐘。

第三個錯誤是沒有先閱讀 GitHub 專案的原始碼、提交紀錄、變更報告、Worker 文件及既有測試紀錄。專案本身已包含多次背景鬧鐘修正、權限同步修正、錯過提醒補發、VAPID 及 Cron 設定等資料。這些資料本應在第一次分析前閱讀，而不是在出現爭議後才閱讀。

第四個錯誤是把未完成實驗的推測講成保證。沒有完整實機數據時，不應使用「百分百」、「一定得」或任何暗示已完成真實測試的說法。尤其是背景通知，必須分開測試 Android、iOS、Chrome、Safari、鎖屏、切換 App、網頁被暫停、省電模式、通知權限變更及網絡中斷。

第五個錯誤是維護沉沒成本。Web Push 已經投入時間，不代表它應該繼續存在。當功能對目前需求沒有增值時，正確決定是停止及移除，而不是為了替已投入的工作辯護，或為它勉強建立新價值。

## 3. 未有做到的驗證

以下驗證在作出可靠承諾前本應完成，但原先沒有以可重現方式完成：

| 應做驗證 | 原先狀況 | 正確要求 |
|---|---|---|
| 本地鬧鐘是否已滿足需求 | 沒有先作功能等價比較 | 先確認本地方案已能提醒，再判斷 Push 是否多餘 |
| 真實手機背景鬧鐘 | 沒有完整實機矩陣 | 指定裝置、系統、瀏覽器及電池狀態逐項記錄 |
| 鎖屏及切換 App | 不能以 Worker 回應代替 | 記錄觸發時間、實際收到時間及失敗情況 |
| 權限被拒絕或改變 | 只處理部分程式分支 | 測試重新授權、取消授權、重裝 PWA 及重新訂閱 |
| Cron、KV、Push service 延遲 | 沒有足夠端到端數據 | 以實際通知時間及伺服器日誌計算延遲 |
| 方案的產品必要性 | 沒有先寫成功條件 | 先寫「沒有此功能會失去甚麼」；若答案是沒有，停止開發 |

## 4. 對使用者造成的時間損失

使用者已明確指出，這項錯誤工程及其反覆修正浪費了「幾日幾夜」的時間。此檔案不虛構精確小時數，因為目前沒有可靠的工時記錄；但損失不是抽象的，至少包括方案設計、前端改動、Worker、Cloudflare 設定、權限及訂閱流程、測試、除錯、重複修正及最後拆除的時間。

因此，不能用「端點仍然可運作」、「測試曾經通過」或「將來或許有用」抵銷這項損失。這些只是技術觀察，不是對時間成本的補償，也不是保留功能的理由。

## 5. 本次移除內容

已由前端移除：

- Web Push Worker URL、VAPID 公開金鑰及 Push 訂閱狀態。
- `pushManager`、Push subscription、`/subscribe` 及 `/vapid-public-key` 呼叫。
- 頁首背景提醒狀態按鈕。
- 背景推送設定組、Worker URL 輸入欄、真正關閉／重新開啟按鈕及相關狀態文字。
- 延遲背景測試按鈕、倒數及測試訊息。
- 開站、新增路線、切換鬧鐘及鬧鐘完成時的背景 Push 同步。

已由 Service Worker 移除：

- `push` 事件處理器。
- 背景推送狀態通知。
- 任何把外部 Push 當成本地鬧鐘必要條件的說明。

已移除專案檔案：

- `worker/` Cloudflare Worker 專案。
- `CLOUDFLARE-BACKGROUND-PUSH-GUIDE.md`。
- `docs/cloudflare-background-push-flow.mmd`。
- `docs/cloudflare-background-push-flow.png`。

## 6. 給其他 AI 的強制參考規則

處理本專案或相似需求時，必須遵守以下規則：

1. **先讀 GitHub，再作判斷。** 使用者要求查 GitHub 時，必須先閱讀原始碼、提交紀錄、Issue、README、變更報告及相關文件。
2. **先定義不可替代的產品需求。** 不可從「這項技術可以做到甚麼」倒推「它應該保留」；要先問它在目前產品中失去後會失去甚麼。
3. **沒有數據就標記為未驗證。** 靜態程式碼、端點 HTTP 200、單元測試通過，都不能代替真實裝置端到端測試。
4. **禁止把可能性講成保證。** 在沒有完整測試矩陣前，不得使用「百分百」、「一定得」或同義承諾。
5. **區分技術可用、功能可用及產品有價值。** 這三者是不同判斷，必須分開報告。
6. **先比較功能重疊，再考慮保留。** 如果本地鬧鐘已完成需求，而 Web Push 只重複提醒，就應判定為冗餘，不應強行賦予價值。
7. **設定停止條件。** 如果功能沒有不可替代價值，或核心實驗失敗，應立即停止擴充並移除，不以沉沒成本作理由。
8. **記錄實際代價。** 若沒有精確工時，應誠實記錄已知的時間範圍或使用者報告，不得自行捏造數字，也不得淡化損失。
9. **事故回應必須有行動。** 道歉不能代替查證、回滾、移除、測試及文件化；回應應指出錯誤、影響、修正及驗證結果。

## 7. 驗證結果

移除後應至少通過以下檢查：

- 前端 JavaScript 語法檢查通過。
- Service Worker 語法檢查通過。
- 搜尋專案前端及 Service Worker，不再存在 Web Push 入口、按鈕、設定或 Push handler。
- 本地鬧鐘的 ETA 計算、計時器、通知及 Service Worker 本地訊息路徑仍然存在。
- 設定頁不再顯示背景推送、Worker、VAPID、訂閱登記或延遲背景測試。

本檔案是失誤記錄，不是為原方案辯護。Web Push 在本專案目前需求下應被視為已否定的冗餘方案。
